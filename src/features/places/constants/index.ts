import type { PlacesState } from '../types/store';

/** Константы фичи мест (карта, брейкпоинты, масштаб). */

/** URL изображения карты. */
export const MAP_IMAGE = '/map.png';

/** Текст для стартовой картинки, над картой по центру. */
export const MAP_INTRO_TEXT = [
  'Поселок городского типа, окруженный где-то бетонным забором, где-то деревянным, где-то металлической сеткой.',
  'Если поехать на юг — Питер (60 км)',
  'Если поехать на север — посёлки, деревни, Старая Ладога (60 км), Петрозаводск (370 км), АРХАНГЕЛЬСК (1000 км), Мурманск (1200 км)',
  'Съезд — рядом старая, уже не используемая по понятным причинам, остановка, разворотная территория, которую можно использовать как парковку.',
  'Главная дорога — кончается просто заросшим асфальтом, переходящим в грунтовку, теряется в лесу, сужаясь до тропинки.',
  'Река — мелководная, грязноватая, обычная лесная, торфяная речка, с пиявками, кувшинками, утками, щуками и карасями.',
];

/** Брейкпоинт мобилки (px). */
export const MOBILE_BREAKPOINT = 768;

/** Базовая ширина канваса на мобилке (px). */
export const MOBILE_BASE_WIDTH = 980;

/** Минимальный масштаб на десктопе. */
export const MIN_SCALE = 1;

/** Минимальный масштаб на мобилке. */
export const MIN_SCALE_MOBILE = 0.6;

/** Максимальный масштаб. */
export const MAX_SCALE = 2;

/** Шаг изменения масштаба. */
export const SCALE_STEP = 0.1;

/** Начальное состояние стора places. */
export const initialState: PlacesState = {
  places: [],
  placeImages: {},
};
